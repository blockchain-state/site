# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real secrets.

# PostgreSQL (arq_postgres container)
POSTGRES_DB=arq_db
POSTGRES_USER=arq_user
POSTGRES_PASSWORD=

# n8n
N8N_ENCRYPTION_KEY=
